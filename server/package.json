{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "npm": "6.x",
    "node": "12.x"
  },
  "scripts": {
    "dev": "nodemon --exec babel-node bin/dev",
    "clean": "if [ -d dist ]; then rm -r dist; fi && mkdir -p dist",
    "build": "npm run clean && babel -d ./dist ./../server -s --ignore 'node_modules'",
    "start": "npm run build && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/node": "^7.6.1",
    "@babel/preset-env": "^7.6.0",
    "concurrently": "^4.1.2",
    "nodemon": "^1.19.2"
  }
}
