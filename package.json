{
  "name": "emaily-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "12.x",
    "npm": "6.x"
  },
  "scripts": {
    "server-dev": "nodemon --exec babel-node bin/dev",
    "client-dev": "cd ./client && npm start",
    "dev": "concurrently --kill-others-on-fail \"npm run server-dev\" \"npm run client-dev\"",
    "clean": "if [ -d dist ]; then rm -r dist; fi && mkdir -p dist",
    "server-build": "npm run clean && babel -d ./dist ./src",
    "client-build": "cd ./client && npm run build",
    "build": "npm run client-build && npm run server-build",
    "start": "export NODE_ENV=test && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-session": "^1.3.3",
    "express": "^4.17.1",
    "mongoose": "^5.7.0",
    "passport": "^0.4.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "nodemon": "^1.19.2",
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/node": "^7.6.1",
    "@babel/preset-env": "^7.6.0"
  }
}
